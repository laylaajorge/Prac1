---
title: "questionThree"
editor: visual
---

```{r echo=5}
yvalues <- cars$dist

xvalues <- cars$speed

ymatrix <- as.matrix(yvalues)

xmatrix <- cbind(1,as.matrix(xvalues))

beta <- solve(t(xmatrix)%*%xmatrix)%*%t(xmatrix)%*%ymatrix

```
